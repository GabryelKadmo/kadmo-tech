"use client";

export default function MobileHeader() {
    // Retorna um componente vazio, sem menu burger nem nenhum link adicional
    // Apenas uma div vazia para não quebrar as referências ao componente em outros lugares
    return (
        <div className="lg:hidden"></div>
    );
}